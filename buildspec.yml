version: 0.1
phases:
  install:
    commands:
      - cd lambda
      - pwd
      - ls
      - zip -r test.zip .
      - aws s3 sync test.zip s3://aadhri-test-buck/
      - cd ..
      - aws cloudformation package --template-file data.yaml --s3-bucket aadhri-test-buck
                                   --output-template-file outputdata.yaml
artifacts:
  type: zip
  files:
    - data.yaml
    - outputdata.yaml
